<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title></title>
</head>
<body>
  <h1>Welcome to the QuoteBoard</h1>
  <div class="container">
    <% if(typeof(errors) != 'undefined' ) { %>
      <% for (var x in errors) { %>
        <h3><%= errors[x].message %></h3>
        <% } %>
      <% } %>
        <form action="/quotes" method="POST">
          <label for="name">Your Name:<input type="text" name="name"></label>
          <label for="quote">Your Quote:<input type="text" name="quote"></label>
          <button>Add my quote</button>
        </form>
    <a href="/quotes"><button>skip to quotes</button></a>
      </div>
    </body>
    </html>
